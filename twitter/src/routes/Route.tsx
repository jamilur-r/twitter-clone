import React from "react";
import { BrowserRouter, Switch } from "react-router-dom";

const Router = () => {
  return (
    <BrowserRouter>
      <div>nav</div>
      <Switch></Switch>
    </BrowserRouter>
  );
};

export default Router;
